import {makeStyles} from '@material-ui/core';

export const useStyles = makeStyles(theme => ({
  root: {
    maxWidth: 345,
  },
  featureImage: {
   width: 296,
    height: 165
  },
  business:{
    fontSize:48,
    fontWeight:800,
    color: '#557DA1'
  }
}))
